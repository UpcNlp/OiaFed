# MOE-FedCL 调试工具快速参考

## 🚀 快速启动

```bash
# 日常快速检查（推荐）
./scripts/simple_debug.sh quick_diagnose

# 完整交互式菜单
./scripts/debug_tools.sh

# 紧急停止所有循环
./scripts/debug_tools.sh emergency_stop
```

## 📋 常用命令速查

| 目的 | 命令 | 说明 |
|------|------|------|
| 快速诊断 | `./scripts/simple_debug.sh quick_diagnose` | 日常检查推荐 |
| 系统诊断 | `./scripts/debug_tools.sh fedcl_diagnose` | 详细系统状态 |
| 分析Warning | `./scripts/debug_tools.sh analyze_warning_details` | Warning详细分析 |
| 分析阻塞 | `./scripts/debug_tools.sh analyze_blocking_root_cause` | 阻塞根本原因 |
| 查看日志 | `./scripts/simple_debug.sh show_log_summary` | 日志摘要 |
| 清理进程 | `./scripts/debug_tools.sh kill_test_processes` | 清理测试进程 |
| 紧急停止 | `./scripts/debug_tools.sh emergency_stop` | 停止所有循环 |

## 🆘 紧急情况处理

```bash
# 1. 终端循环输出不停
Ctrl+C  # 或者输入 'q'

# 2. 进程无法清理
./scripts/debug_tools.sh emergency_stop

# 3. 脚本无权限
chmod +x ./scripts/*.sh

# 4. 强制杀死进程
ps aux | grep pytest
kill -9 <pid>
```

## 🔍 故障诊断流程

```
1. 快速诊断     → ./scripts/simple_debug.sh quick_diagnose
2. 系统详细诊断 → ./scripts/debug_tools.sh fedcl_diagnose  
3. Warning分析  → ./scripts/debug_tools.sh analyze_warning_details
4. 阻塞分析     → ./scripts/debug_tools.sh analyze_blocking_root_cause
5. 清理进程     → ./scripts/debug_tools.sh kill_test_processes
```

## 📊 日志文件位置

```
logs/experiment_YYYYMMDD_HHMMSS/
├── federated_training.log     # 主要日志
├── server_test_server.log     # 服务器日志  
└── clients/client_*.log       # 客户端日志
```

## ⚠️ 注意事项

- 🟢 **推荐**: 日常使用 `simple_debug.sh`
- 🟡 **谨慎**: 实时监控功能避免长时间运行
- 🔴 **记住**: Ctrl+C 和 emergency_stop 是安全出口
