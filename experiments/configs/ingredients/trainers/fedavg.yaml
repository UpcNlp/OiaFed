# FedAvg 训练器配置
# experiments/configs/ingredients/trainers/fedavg.yaml

trainer:
  name: "FedAvgMNIST"
  params:
    max_rounds: 10
    min_clients: 5
    client_selection_ratio: 1.0
    local_epochs: 1
    learning_rate: 0.01
    batch_size: 32
