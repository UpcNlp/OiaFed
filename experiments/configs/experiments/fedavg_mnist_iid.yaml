# FedAvg + MNIST + IID 完整实验配置
# experiments/configs/experiments/fedavg_mnist_iid.yaml

experiment_name: "fedavg_mnist_iid"

# 复用配置组件
includes:
  - "../ingredients/datasets/mnist.yaml"
  - "../ingredients/trainers/fedavg.yaml"
  - "../ingredients/communication/memory.yaml"

# 覆盖特定参数（可选）
overrides:
  trainer:
    params:
      max_rounds: 20
  dataset:
    partition:
      num_clients: 5
