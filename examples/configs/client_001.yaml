# ============================================
# 客户端1配置（继承 base_client.yaml）
# 只需要覆盖差异化的部分
# ============================================

extends: "base_client.yaml"

communication:
  node_id: "client_001"     # 只改 ID

  # 如果需要自定义某些字段，也可以覆盖
  # transport:
  #   timeout: 60.0
