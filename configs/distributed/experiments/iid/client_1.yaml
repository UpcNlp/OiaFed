# IID实验 - 客户端1配置
# configs/distributed/experiments/iid/client_1.yaml

extends: "../../base/client_base.yaml"

# 节点ID
node_id: "client_1"

# 快速批量实验配置
training:
  learner:
    params:
      batch_size: 128
      local_epochs: 1
