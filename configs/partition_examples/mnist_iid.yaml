# MNIST IID 划分示例
# 使用场景：均匀随机划分，所有客户端数据分布一致

dataset:
  type: mnist
  args:
    data_dir: ./data
    partition_id: 0
    num_partitions: 5
    partition_type: iid
    partition_config:
      seed: 42  # 设置随机种子以保证可重复性
    download: true
